The rest api offer 3 functions: 
- get(name): allows the user to get the wanted cookie 
- set(name, value, attibutes): allows the user to set a new cookie with the wanted attibutes
- remove(name): allows the user to remove a wanted cookie